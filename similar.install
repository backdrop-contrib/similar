<?php
// $Id$

/**
 * Add FULLTEXT index to MySQL tables.
 * Module does not support, pgsql, so no changes for it
 */
function similar_install() {
  switch ($GLOBALS['db_type']) {
    case 'mysql':
    case 'mysqli':
      db_query('ALTER TABLE {node_revisions} ADD FULLTEXT(title, body)');
  }
}
